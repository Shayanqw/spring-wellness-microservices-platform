name: CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        service:
          - api-gateway
          - wellness-service
          - goal-tracking-service
          - event-service

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      - name: Build (${{ matrix.service }}) - compile only
        working-directory: ${{ matrix.service }}
        run: |
          chmod +x ./gradlew
          ./gradlew clean build -x test --no-daemon
